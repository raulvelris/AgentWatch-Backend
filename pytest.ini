[pytest]
# El paquete `app` vive en la raíz del repo. Sin esto, `pytest tests/` (lo que
# corre ci.yml) falla con ModuleNotFoundError: No module named 'app', porque el
# console-script de pytest no agrega el cwd a sys.path (a diferencia de `python -m pytest`).
pythonpath = .
testpaths = tests
